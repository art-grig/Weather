'use strict';

describe('myApp.postcodeView module', function() {

  beforeEach(module('myApp.postcodeView'));

  describe('postcodeView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var postcodeCtrl = $controller('PostcodeCtrl');
      expect(postcodeCtrl).toBeDefined();
    }));

  });
});